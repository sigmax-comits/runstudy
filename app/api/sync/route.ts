import {NextRequest,NextResponse} from "next/server";
import crypto from "crypto";
function secret(){return process.env.STUDY_X_AUTH_SECRET||"dev-only-change-me"}
function sign(v:string){return crypto.createHmac("sha256",secret()).update(v).digest("hex")}
function current(req:NextRequest){const t=req.cookies.get("study_x_session")?.value;if(!t)return null;const [p,s]=t.split(".");if(!p||s!==sign(p))return null;try{const x=JSON.parse(Buffer.from(p,"base64url").toString());return x.exp>Date.now()?x.u:null}catch{return null}}
async function kv(command:string[]){const url=process.env.KV_REST_API_URL,token=process.env.KV_REST_API_TOKEN;if(!url||!token)throw new Error("Cloud storage is not configured");const r=await fetch(url,{method:"POST",headers:{Authorization:"Bearer "+token,"Content-Type":"application/json"},body:JSON.stringify(command)});if(!r.ok)throw new Error("Storage request failed");return r.json()}
export async function GET(req:NextRequest){const u=current(req);if(!u)return NextResponse.json({error:"Not logged in"},{status:401});try{const x=await kv(["GET","studyx:user:"+u]);return NextResponse.json({username:u,data:x.result?JSON.parse(x.result):{progress:{},tasks:[],activeTimer:null}})}catch(e){return NextResponse.json({error:"Cloud storage is not configured"},{status:503})}}
export async function POST(req:NextRequest){const u=current(req);if(!u)return NextResponse.json({error:"Not logged in"},{status:401});try{const body=await req.json();const key="studyx:user:"+u;const existingRaw=await kv(["GET",key]);let existing:any={progress:{},tasks:[]};try{if(existingRaw.result)existing=JSON.parse(existingRaw.result)}catch{}const hasProgress=body.progress&&typeof body.progress==="object"&&!Array.isArray(body.progress);const hasTasks=Array.isArray(body.tasks);const hasActiveTimer=Object.prototype.hasOwnProperty.call(body,"activeTimer");const data={progress:hasProgress?{...(existing.progress||{}),...body.progress}:(existing.progress||{}),tasks:hasTasks?body.tasks:(Array.isArray(existing.tasks)?existing.tasks:[]),activeTimer:hasActiveTimer?body.activeTimer:(existing.activeTimer??null)};await kv(["SET",key,JSON.stringify(data)]);await kv(["ZADD","studyx:leaderboard","GT",String(data.progress?.seconds||0),u]);return NextResponse.json({ok:true,data})}catch{return NextResponse.json({error:"Cloud storage is not configured"},{status:503})}}
