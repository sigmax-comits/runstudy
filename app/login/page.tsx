"use client";
import {useState} from "react";
import {useRouter} from "next/navigation";
export default function Login(){const [u,setU]=useState(""),[p,setP]=useState(""),[err,setErr]=useState(""),[busy,setBusy]=useState(false),router=useRouter();async function login(){setBusy(true);setErr("");try{const r=await fetch("/api/auth",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:u,password:p})});const x=await r.json();if(!r.ok)throw new Error(x.error||"Login failed");router.replace("/timer")}catch(e){setErr(e instanceof Error?e.message:"Login failed")}finally{setBusy(false)}}return <main className="login-page"><div className="login-card glass-panel"><div className="brand login-brand"><span className="brand-mark">SX</span><b>Study<span> X</span></b></div><span className="section-kicker">PRIVATE STUDY SPACE</span><h1>Welcome back.</h1><p>Log in to access your timer, tasks, progress and leaderboard.</p><input value={u} onChange={e=>setU(e.target.value)} placeholder="Username"/><input value={p} onChange={e=>setP(e.target.value)} placeholder="Password" type="password" onKeyDown={e=>e.key==="Enter"&&login()}/>{err&&<div className="login-error">{err}</div>}<button className="login-submit" onClick={login} disabled={busy}>{busy?"Signing in…":"Sign in"}</button></div></main>}
